import {Table} from "semantic-ui-react";
import {CommonSummaryProperties} from "@/banners/summary";

export default function AvgDaysBetween(
    {
        versionParts,
        banners,
        type,
        order,
        date,
        filterText,
    }: CommonSummaryProperties
) {
    return (
        <Table.Body>
        </Table.Body>
    )
}